@AGENTS.md

Before editing Next.js code, use the installed documentation under `node_modules/next/dist/docs/` as the authoritative version-specific reference. Keep Claude's plan and handoff synchronized with `SESSION.md` and `TODO.md`. Browser automation may activate an employer's submit control only through the opt-in "Auto-fill & submit" mode described in AGENTS.md's Project purpose section — never as a side effect of the default "Auto-fill (review)" mode, and never by loosening that mode's fallback-to-review-on-uncertainty behavior.
